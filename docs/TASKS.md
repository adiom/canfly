# Задачи (Features)

Авто-сгенерировано из GitHub Issues. Не редактировать вручную.
Синхронизировано: 21 августа 2026 г.

---

### Feature: #13 — Кэширование частых запросов к БД
- Приоритет: `priority-medium`
- Статус: `open`
- Обновлено: 21.08.2026

Описание

Страницы /releases и /release/[slug] делают множественные запросы к БД без кэширования. Особенно заметно на Neon (холодный старт).

Где

- lib/server/releases.ts — каждый запрос к /releases делает 3-4 SELECT
- components/release-page.tsx — загрузка релиза + издания + главы + серии

Варианты решения

- unstable_cache() из Next.js для серверных фетчей (in-memory, разделяемый)
- Redis (Upstash) для persistent cache
- Минимум: deduplication запросов через React cache() или Next.js fetch caching

Критерии

- Не кэшировать данные пользователя (сессии, корзина)
- Инвалидация при…

---

### Feature: #12 — Полнотекстовый поиск по главам релизов
- Приоритет: `priority-medium`
- Статус: `open`
- Обновлено: 02.07.2026

Описание

Текущий поиск (lib/server/search.ts) ищет только по метаданным: title, annotation, genre. Содержимое глав не индексируется.

Что нужно

- Полнотекстовый индекс по таблице chapters (content_markdown)
- PostgreSQL tsvector + tsquery или pg_trgm
- Результаты — ссылка на конкретную главу релиза
- Подсветка совпадений в сниппете
- Фильтр: искать в рамках релиза (локально) или по всем релизам (глобально)

---

### Feature: #10 — Реакции к постам персонажей (лайки/комментарии)
- Приоритет: `priority-medium`
- Статус: `open`
- Обновлено: 02.07.2026

Описание

Упомянуто в v2.0 «Следующие шаги». Посты персонажей (character_posts) сейчас read-only — нельзя лайкнуть или ответить.

Что нужно

- Таблица character_post_likes (user_id, post_id, created_at)
- Таблица character_post_replies (раскомментировать/создать)
- Кнопка «❤️» под постом
- Счётчик лайков
- Авторизация: ставить лайк может только зарегистрированный пользователь

---

### Feature: #8 — Система комментирования релизов
- Приоритет: `priority-medium`
- Статус: `open`
- Обновлено: 02.07.2026

Описание

Таблица comments есть в схеме 002_release_system.sql, но UI и API для комментирования не реализованы.

Что нужно

- Компонент components/release-comments.tsx
- API: CRUD комментариев
- Только авторизованные пользователи
- Вложенные ответы (threaded comments)
- Редактирование/удаление своих комментариев
- Модерация: админ/автор релиза может удалить любой комментарий

---

### Feature: #9 — Закладки (bookmarks) для релизов
- Приоритет: `priority-low`
- Статус: `open`
- Обновлено: 21.08.2026

Состояние на 21 августа 2026

Задача переформулирована: часть описанного сделана, но не через таблицу bookmarks, и остаток выглядит иначе.

Что уже есть:

- components/bookmarks-panel.tsx подключён к обоим ридерам (release-book-reader.tsx:1008, spread-reader.tsx:1317). Это панель хайлайтов: работает с ChapterHighlight, умеет заметку, публичность и удаление — то есть пункт «поддержка заметок» закрыт хайлайтами.
- Сохранение и чтение — lib/server/chapter-highlights.ts, шаринг цитаты — /highlight/[id].

Чего нет:

- Таблица bookmarks (postgres/002_release_system.sql:167) мертва: 0 строк, ни…

---

